---
title: sdwan-concepts
---

!!! tip "Reference Lab"
    SD-WAN mechanisms built by hand — two transports, GRE overlay per color, DSCP-based path policy, probe-driven failover — with every piece mapped to the Catalyst SD-WAN component that automates it

!!! note "Images"
    `docker build -t sdwan-lab:local labs/sdwan-concepts/` (no cEOS required)

{%
  include-markdown "../../../labs/sdwan-concepts/README.md"
%}
