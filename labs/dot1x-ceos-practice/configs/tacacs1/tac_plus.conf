key = "labkey"

accounting file = /var/log/tacacs_accounting.log

group = neteng-admin {
    default service = permit
    service = exec {
        priv-lvl = 15
    }
}

user = neteng {
    login = cleartext "labpass"
    member = neteng-admin
}
