# Ticket TR-306 — Voice-only portal timeout

**Reported by:** Unified communications team  
**Impact:** Voice-network endpoints cannot open the provisioning portal, while
corporate users can use it normally.  
**Symptom:** Affected endpoints resolve `web.range.test` correctly and can
reach the service address, but TCP connections to the portal time out. The
problem is isolated to the voice source network.

Diagnose, fix, and verify the incident. Submit request/return-path, service,
and policy evidence, the minimal correction, and tests from affected and
unaffected client networks.
