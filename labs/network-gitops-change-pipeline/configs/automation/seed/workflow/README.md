# Student-owned workflow policy

Create `change-policy.yml` before review or deployment. The engine is supplied
plumbing; this file is the withheld transaction, rollback, verification, and
drift-reconciliation contract that makes the plumbing safe to execute.
