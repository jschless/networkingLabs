! core1 — AS65300 internal core router  (Arista EOS)
! Role: primary campus/DC core, directly connected to server subnet
!
! Runs OSPF area 0 with edge and core2.
! Redistributes or originates 10.100.0.0/30 (server subnet) via OSPF.
! OSPF default-information from edge propagates the internet default
! down to core1, which then serves it to the server subnet via its
! connected route / gateway-of-last-resort.
!
! Links:
!   Ethernet1: 10.255.1.2/30   (to edge)
!   Ethernet2: 10.255.3.1/30   (to core2 — redundant core interconnect)
!   Ethernet3: 10.100.0.1/30   (to server)
!
! Loopback0: 10.0.0.2/32

hostname core1
!
interface Loopback0
   ip address 10.0.0.2/32
!
interface Ethernet1
   description to-edge  10.255.1.0/30
   no switchport
   ip address 10.255.1.2/30
   no shutdown
!
interface Ethernet2
   description to-core2  10.255.3.0/30
   no switchport
   ip address 10.255.3.1/30
   no shutdown
!
interface Ethernet3
   description to-server  10.100.0.0/30
   no switchport
   ip address 10.100.0.1/30
   no shutdown
!
ip routing
!
router ospf 1
   router-id 10.0.0.2
   passive-interface default
   no passive-interface Ethernet1
   no passive-interface Ethernet2
   network 10.0.0.2/32 area 0.0.0.0
   network 10.255.1.0/30 area 0.0.0.0
   network 10.255.3.0/30 area 0.0.0.0
   network 10.100.0.0/30 area 0.0.0.0
!
end
