# LDAP Account Manager (LAM) configuration hints
#
# LAM is configured via its web UI on first launch.
# Use these values when prompted:
#
#   LDAP server:     ldap://dc1.lab.corp
#   Tree suffix:     DC=lab,DC=corp
#   Admin user:      CN=Administrator,CN=Users,DC=lab,DC=corp
#   Admin password:  P@ssw0rd1
#
# LAM profile password (to edit LAM config): lam
#
# After provisioning the domain in Task 1, open LAM at:
#   http://<docker-host-ip>:8080
#
# Navigate to: LAM Configuration → Edit Server Profiles → lam
# Set the LDAP connection details above, then save.
