# Squid proxy for lab.corp — YOU complete this file.
#
# Pre-provided for you (by the container's join script):
#   * proxy1 is joined to the domain
#   * /etc/squid/PROXY.keytab holds the HTTP/proxy1.lab.corp@LAB.CORP service key
#     AND the machine account key (used by the group helper to bind to LDAP)
#   * KRB5_KTNAME points Squid's helpers at that keytab
#
# Your job (see the README tasks):
#   Task 2 — turn on Kerberos Negotiate authentication
#   Task 3 — enforce per-AD-group access with an external ACL
#
# After every edit, reload without dropping connections:
#   docker exec proxy1 squid -k reconfigure
# ===========================================================================

http_port 3128

# --- Task 2: Kerberos Negotiate authentication -----------------------------
# Add an `auth_param negotiate` block using the helper
#   /usr/lib/squid/negotiate_kerberos_auth
# pointed at the keytab (-k) and the service principal (-s). Then require it
# with an `acl ... proxy_auth REQUIRED`.
#
# TODO: auth_param negotiate program ...
# TODO: acl authenticated proxy_auth REQUIRED

# --- Task 3: AD group-based access -----------------------------------------
# Use /usr/lib/squid/ext_kerberos_ldap_group_acl with `-g <group>` to test the
# authenticated user's AD group, define a matching `acl ... external ...`, and
# write http_access rules so:
#   * engineering  -> unrestricted
#   * everyone else (e.g. finance) -> allowed EXCEPT webserver2.lab.corp
#
# TODO: external_acl_type ... ext_kerberos_ldap_group_acl -g engineering
# TODO: acl in_engineering external ...
# TODO: acl site_blocked dstdomain webserver2.lab.corp
# TODO: http_access rules (order matters!)

# Until you add auth + policy above, the proxy denies everything:
http_access deny all

# --- Given scaffolding (leave as-is) ---------------------------------------
cache deny all
coredump_dir /var/spool/squid
access_log /var/log/squid/access.log
visible_hostname proxy1.lab.corp
